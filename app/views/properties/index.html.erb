<h1>Checkout these properties for sale:</h1>

<div>
    <ul>
        <% @properties.each do |p| %>
        <img src="<%= p.image %>" alt="" width="300px", height="200px"> 
        <li>Price($): <%= p.price %></li>
        <li>Address: <%= p.address %></li>
        <li><%= link_to 'More details here', property_path(p.id) %></li>
        <% end %>
    </ul>
</div>
